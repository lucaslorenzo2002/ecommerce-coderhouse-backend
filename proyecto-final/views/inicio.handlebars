<a href="/api/carrito">ir al carrito</a>
<button onclick="location.href='/api/auth/logout'">logout</button>

<div>
    <table>
        <tr> 
            <th>nombre</th><th>precio</th>
    {{#each productos}}
        <tr>
            <td>{{this.nombre}}</td>
            <td>{{this.precio}}</td>
            <form action="/api/agregaralcarrito/{{this._id}}" method="post">
            <td><input type="submit" value="agregar al carrito"></td>
            </form>
        </tr>    
    {{/each}} 
        </tr> 
    </table>
</div>  